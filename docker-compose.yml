version: '3.7'

services:
  node_api: 
    build: .
    ports: 
      - "49160:3000"
    volumes:
      - /home/rodrigo/Projetos/movie-db-api:/app
    networks:
      - dev_network

  flask_api:
    build: 
      context: src/services/api
      dockerfile: Dockerfile 
    ports:
      - "4191:5000"

    networks:
      - dev_network

networks:
  dev_network: